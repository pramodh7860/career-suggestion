// Main server entry point
// This file serves as the entry point and starts the backend server

// Import the backend server
const backendServer = require('./backend/server.js');

// The backend server will handle all the routing and middleware
// This file just provides a clean entry point for the application

console.log('Career Compass Server Starting...');
console.log('Backend server imported successfully');

// The backend server.js file will handle starting the Express app
// No additional code needed here as the backend server.js is self-contained
